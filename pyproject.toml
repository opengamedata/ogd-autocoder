[project]
name = "ogd-autocoder"
version = "0.1.0"
description = "Tool for human coding of Event data, with integration to feature extraction and automated building of models to apply codes to new data."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.1",
    "matplotlib>=3.10.3",
    "numpy>=1.26.4",
    "pandas>=2.3.1",
    "scikit-learn>=1.7.0",
    "tensorflow>=2.16.2",
]

[dependency-groups]
dev = [
    "dask>=2025.7.0",
    "statsmodels>=0.14.5",
    "nbformat>=5.10.4",
    "ipykernel>=6.29.5",
    "openpyxl>=3.1.5",
    "plotly>=6.2.0",
    "pyarrow>=20.0.0",
]